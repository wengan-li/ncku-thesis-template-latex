%
% V1 public API compatibility adapter.
%
% The complete 2.x line keeps generic/deprecated compatibility declarations here.
% NCKU college/department presets are loaded only by the NCKU profile: unchanged
% 1.x NCKU projects still select that default profile, while another institution
% does not inherit NCKU catalogue data.
%
\input{./template/compat/deprecated}
