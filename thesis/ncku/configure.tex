% ------------------------------------------------

\documentclass[12pt, a4paper]{report}

% ------------------------------------------------

% XeLaTex 的設定

% --------------------------
% 加這個就可以設定字體
\usepackage{fontspec}

% 使用xeCJK，其他的還有CJK或是xCJK
\usepackage[BoldFont]{xeCJK}

% Set english font (Use package <ifplatform> to check)
\usepackage{ifplatform}
\ifwindows
  % In Windows
  \setmainfont{Times New Roman}
\else
  % In *nix
  \usepackage{times}
\fi 

% 設定中英文的字型
% 字型的設定可以使用系統內的字型，而不用像以前一樣另外安裝
\setCJKmainfont{標楷體}

% 中文自動換行
\XeTeXlinebreaklocale "zh"

% 文字的彈性間距
\XeTeXlinebreakskip = 0pt plus 1pt

% 設定段落之間的距離
\setlength{\parskip}{0.3cm}

% 設定行距
\linespread{1.5}\selectfont

% ------------------------------------------------

% LaTex package

% --------------------------

\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{pdfpages}
\usepackage{url}
\usepackage{multirow}
\usepackage{fp}
\usepackage{siunitx}
\usepackage{ifthen}
\usepackage{xparse}
\newcommand{\tabincell}[2]{\begin{tabular}{@{}#1@{}}#2\end{tabular}}

% ------------------------------------------------

% 有關學校對論文要求的設定

% --------------------------

% 學校排版 Arrangement style
%\usepackage{./ncku/style/macros}
\usepackage{./ncku/style/utdiss}

% --------------------------

% 一些用來設定用的function和variable
\input{./ncku/command.tex}

% --------------------------

% 論文有關資料
\input{./conf/conf.tex}

% --------------------------

% 設定學校浮水印 Watermark
\UseSchoolWatermark

% --------------------------

% 使用 hyperref 在 pdf 簡介欄裡填入相關資料
\ifx \hypersetup \undefined
  % do nothing
  \relax
\else
  \ifx \GetChiTitle \undefined
    \hypersetup
    {
      pdftitle  = {\GetEngTitle},
      pdfauthor = {\GetAuthorEngName},
    }
  \else
    \hypersetup
    {
      pdftitle  = {\GetEngTitle \enspace (\GetChiTitle)},
      pdfauthor = {\GetAuthorEngName \enspace (\GetAuthorChiName)},
    }
  \fi

  \hypersetup
  {
    unicode     = true,
    pdfcreator  = {\GetSchoolEngName},
    pdfproducer = {\GetSchoolEngName},
    pdfsubject  = {},
    pdfkeywords = {},
  }
\fi

% --------------------------

% 這論文的版本
\input{./ncku/version.tex}

% ------------------------------------------------

